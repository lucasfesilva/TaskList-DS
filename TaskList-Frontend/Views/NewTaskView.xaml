<Window x:Class="TaskList_Frontend.Views.NewTaskView"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskList_Frontend.Views"        
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Height="350" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Título:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="0"
                 Margin="0,20,0,10"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />


        <TextBlock Grid.Row="1" Text="Descrição:" FontWeight="Bold" Margin="0,5,0,5"/>
        <TextBox Grid.Row="1"
                 Margin="0,25,0,10"
                 Height="60"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Text="Data de Conclusão:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <xctk:DateTimePicker 
                 Value="{Binding Task.DoneAt, Mode=TwoWay}" 
                 Format="Custom" 
                    FormatString="dd/MM/yyyy HH:mm"
                Width="200"/>

        </StackPanel>


        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Text="Status:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding StatusList}"
                      SelectedItem="{Binding SelectedStatus, UpdateSourceTrigger=PropertyChanged}"
                      IsEnabled="{Binding IsStatusEnabled}"
                      Width="150"/>
        </StackPanel>


        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,0">
            <Button Content="Salvar" Width="50" Margin="0,0,10,0" Command="{Binding SaveCommand}" Background="LightGreen" BorderThickness="0"/>
            <Button Content="Fechar" Width="50" Command="{Binding CancelCommand}" Background="IndianRed" BorderThickness="0"/>
            
        </StackPanel>

    </Grid>
</Window>

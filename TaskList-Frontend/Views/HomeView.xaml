<Window x:Class="TaskList_Frontend.Views.HomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskList_Frontend.Views"
        mc:Ignorable="d"
        Height="800"
        Width="1150"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"> 
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
               Text="Task Manager" 
               FontSize="24" 
               FontWeight="Bold" 
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,10">
            <Button Content="Nova Tarefa"
            Command="{Binding OpenNewTaskWindowCommand}"
            Width="100"
            Height="32"
            Margin="5"
            Background="LightGreen"
            BorderThickness="0"
            HorizontalAlignment="Left"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,0">
                <TextBlock Text="Filtrar:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="150" Height="32"
                  ItemsSource="{Binding StatusList}"
                  SelectedItem="{Binding SelectedStatus}"/>
            </StackPanel>
        </DockPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    
                    <GridViewColumn Header="Título" DisplayMemberBinding="{Binding TaskTitle}" Width="200"/>
                    
                    <GridViewColumn Header="Descrição" DisplayMemberBinding="{Binding TaskDescription}" Width="300"/>
                    
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding StatusText}" Width="100"/>

                    <GridViewColumn Header="Criada em" DisplayMemberBinding="{Binding CreatedAtFormatted}" Width="150"/>

                    <GridViewColumn Header="Finalizada em" DisplayMemberBinding="{Binding DoneAtFormatted}" Width="150"/>
                    
                    <GridViewColumn Header="Ações" Width="150">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Editar" Margin="2" Background="DeepSkyBlue" BorderThickness="0"
                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding Id}"/>
                                    <Button Content="Excluir" Margin="2" Background="IndianRed" BorderThickness="0"
                                Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding Id}"/>
                    </StackPanel>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
                    
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
